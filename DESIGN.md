# Pool's Day — 디자인 가이드라인

> *수영 바보들을 위한 수영장 지도*

이 문서는 Pool's Day의 디자인 철학과 기초 시스템을 정의합니다. 컴포넌트를 만들거나 카피를 쓸 때, 항상 이 문서로 돌아와 결을 맞추세요.

---

## 0. 디자인 철학

### 한 줄로
**친근하지만 정돈된, 위트 있지만 유치하지 않은.**

### 5가지 원칙

**1. 친근함 > 차가움**
- 형광 블루·짙은 그레이 같은 *기능적 차가움*은 피한다
- 크림·풀 블루·따뜻한 액센트로 *말 거는 느낌*을 만든다
- 흰색 대신 *살짝 따뜻한 오프화이트*

**2. 위트는 디테일로**
- 메인 UI는 깔끔하게, 위트는 *마스코트 / 카피 / 빈 상태 / 푸시*에서만 살짝 드러낸다
- 위트가 모든 곳에 있으면 시끄러워진다 — *발견하는 재미*가 핵심
- 90% 정돈 + 10% 장난 = 좋은 비율

**3. 정보보다 결정**
- 사용자는 *수영 어디 갈지* 정하러 옴 — 정보 나열 아니라 *결정 도와주기*
- 카드 한 장에 너무 많이 담지 않기
- 가까운 풀 3개부터 보여주는 게 30개 보여주는 것보다 낫다

**4. 한국과 영어가 같이**
- *Pool's Day / 풀스데이* — 두 언어가 자연스럽게 섞이는 디자인
- 영문 헤딩과 국문 본문이 같은 화면에 섞여도 *조화롭게*
- 폰트 페어링이 핵심

**5. 매일 열어도 좋은 앱**
- 화려한 인트로 X. 첫 화면 = 핵심 기능
- 매일 보는 앱이라 *덜 자극적이고 더 편안한* 톤
- 모션은 짧고 부드럽게

---

## 1. 컬러 시스템

### 1-1. 메인 팔레트

```css
:root {
  /* === 풀 블루 (메인) === */
  --pool-50:    #F0F9FF;   /* 가장 옅은 배경 */
  --pool-100:   #E0F2FE;   /* 호버 배경 */
  --pool-200:   #BAE6FD;   /* 보조 배경 */
  --pool-400:   #38BDF8;   /* 보조 액센트 */
  --pool-500:   #0EA5E9;   /* 메인 — 버튼, 링크, 마커 */
  --pool-600:   #0284C7;   /* 호버 상태 */
  --pool-700:   #0369A1;   /* 진한 헤딩 */
  --pool-900:   #0C4A6E;   /* 가장 진한 강조 */

  /* === 위트 액센트 === */
  --fool-yellow:   #FCD34D;   /* "바보같음" 하이라이트, 마스코트 */
  --fool-yellow-deep: #F59E0B; /* 호버, 보더 */

  /* === 감정 컬러 === */
  --warm-coral:    #FB7185;   /* 찜, 하트 */
  --warm-coral-bg: #FFE4E6;   /* 찜한 카드 배경 */

  /* === 중성 (텍스트·배경) === */
  --bg-cream:      #FAFAF7;   /* 메인 배경 — 살짝 따뜻한 오프화이트 */
  --bg-paper:      #FFFFFF;   /* 카드 배경 */
  --bg-subtle:     #F5F5F2;   /* 비활성 영역 */

  --ink-900:       #0F172A;   /* 본문, 헤딩 */
  --ink-700:       #334155;   /* 강조 본문 */
  --ink-500:       #64748B;   /* 보조 텍스트 */
  --ink-400:       #94A3B8;   /* 캡션, 비활성 */

  --line-default:  #E5E7EB;   /* 기본 보더 */
  --line-subtle:   #F1F5F9;   /* 옅은 구분선 */
}
```

### 1-2. 사용 규칙

| 컬러 | 어디에 |
|---|---|
| `--pool-500` | 메인 버튼, 링크, 활성 상태, 실내 풀 마커 |
| `--pool-700` | 헤딩, 진한 텍스트 |
| `--pool-100` | 활성 칩 배경, 호버 효과 |
| `--fool-yellow` | 마스코트 포인트, *Pool's Day* 한정 카피 강조, 별 아이콘 |
| `--warm-coral` | 찜 하트, 즐겨찾기 액션, 야외 풀 마커 |
| `--bg-cream` | 메인 배경 (절대 순백 #FFFFFF 쓰지 말 것) |
| `--bg-paper` | 카드, 모달 |

### 1-3. 사용 비율 (60-30-10)

- **60%** 중성 (크림 배경 + 텍스트)
- **30%** 풀 블루 (액센트, 인터랙션)
- **10%** 위트 액센트 (옐로 + 코랄)

이 비율이 깨지면 *시끄럽거나 차갑게* 느껴짐.

### 1-4. 다크모드 (Phase 2)

```css
@media (prefers-color-scheme: dark) {
  :root {
    --bg-cream:    #0F172A;
    --bg-paper:    #1E293B;
    --ink-900:     #F1F5F9;
    --ink-500:     #94A3B8;
    --pool-500:    #38BDF8;  /* 어두운 배경에선 살짝 밝게 */
  }
}
```

---

## 2. 타이포그래피

### 2-1. 폰트 스택

```css
/* 본문·UI (한글) */
font-family: 'Pretendard Variable', 'Pretendard', -apple-system,
             BlinkMacSystemFont, system-ui, sans-serif;

/* 강조·로고 (영문) */
font-family: 'Fraunces', 'Pretendard Variable', serif;
```

**왜 이 조합?**
- **Pretendard** — 한국어 가독성 최고, 무료, 가변 폰트. 차분하지만 딱딱하지 않음
- **Fraunces** — 가변 세리프, 영문 헤딩에 *위트와 캐릭터*를 더함. *Pool's Day* 로고에 이상적
- 두 폰트 모두 가변(variable) 지원으로 weight 자유롭게 조절

### 2-2. 텍스트 스케일

```css
/* 디스플레이 (랜딩, 큰 헤딩) */
.text-display    { font: 700 48px/1.1 'Fraunces';  letter-spacing: -0.02em; }

/* 헤딩 */
.text-h1         { font: 700 32px/1.2 'Pretendard'; letter-spacing: -0.02em; }
.text-h2         { font: 600 24px/1.3 'Pretendard'; letter-spacing: -0.01em; }
.text-h3         { font: 600 20px/1.4 'Pretendard'; }
.text-h4         { font: 600 17px/1.4 'Pretendard'; }

/* 본문 */
.text-body       { font: 400 16px/1.6 'Pretendard'; }
.text-body-bold  { font: 600 16px/1.6 'Pretendard'; }
.text-body-sm    { font: 400 14px/1.5 'Pretendard'; }

/* 캡션·라벨 */
.text-caption    { font: 500 13px/1.4 'Pretendard'; color: var(--ink-500); }
.text-label      { font: 600 12px/1 'Pretendard'; letter-spacing: 0.04em; text-transform: uppercase; }

/* 이탤릭 강조 (위트 카피) */
.text-witty      { font: 400 italic 16px/1.6 'Fraunces'; }
```

### 2-3. 한·영 페어링 규칙

- 헤더 로고: `Pool's Day` (Fraunces, italic) + 작은 *풀스데이* (Pretendard)
- 한글 헤딩 + 영문 본문 섞을 때: 영문은 `Pretendard` (Fraunces 안 씀, 너무 다름)
- 위트 한 줄 (영문): `Fraunces italic` — *"No fooling. Just swimming."*
- 한국어 위트: `Pretendard 500 italic` — *수영 안 하면 안 되는 분들께.*

---

## 3. 레이아웃 & 스페이싱

### 3-1. 8pt 그리드

모든 간격은 **8의 배수** 또는 **4의 배수**(미세 조정).

```
4, 8, 12, 16, 24, 32, 48, 64, 96, 128
```

### 3-2. 컨테이너 폭

```css
--container-sm:  640px;   /* 폼, 글 */
--container-md:  768px;   /* 카드 리스트 */
--container-lg:  1024px;  /* 메인 콘텐츠 */
--container-xl:  1280px;  /* 데스크톱 max */
```

### 3-3. 패딩·마진 표준

| 위치 | 값 |
|---|---|
| 페이지 좌우 패딩 (모바일) | 20px |
| 페이지 좌우 패딩 (데스크톱) | 32~48px |
| 카드 내부 패딩 | 16~20px |
| 카드 사이 간격 | 16px |
| 섹션 사이 간격 | 48~64px |
| 헤더 높이 | 64px (모바일) / 72px (데스크톱) |

### 3-4. 반응형 브레이크포인트

```css
--breakpoint-sm:  640px;   /* 큰 모바일 */
--breakpoint-md:  768px;   /* 태블릿 */
--breakpoint-lg:  1024px;  /* 데스크톱 */
--breakpoint-xl:  1280px;  /* 큰 데스크톱 */
```

**모바일 우선.** 데스크톱은 풍부한 정보, 모바일은 핵심 행동에 집중.

### 3-5. 모서리 둥글기

```css
--radius-sm:    6px;    /* 칩, 작은 버튼 */
--radius-md:    10px;   /* 입력 필드, 보통 버튼 */
--radius-lg:    14px;   /* 카드 */
--radius-xl:    20px;   /* 모달, 큰 카드 */
--radius-pill:  9999px; /* 칩, 토글 */
```

⚠️ **너무 동글하면 유치해짐.** 16px 넘으면 신중히. 10~14px가 sweet spot.

### 3-6. 그림자

```css
--shadow-sm:  0 1px 2px rgba(15, 23, 42, 0.04);
--shadow-md:  0 2px 12px rgba(14, 165, 233, 0.08);   /* 풀 블루 톤! */
--shadow-lg:  0 8px 24px rgba(14, 165, 233, 0.12);
--shadow-pop: 0 12px 36px rgba(14, 165, 233, 0.15); /* 모달, 호버 강조 */
```

⚠️ 회색 그림자(`rgba(0,0,0,…)`) 대신 *살짝 풀 블루 띠는* 그림자를 써서 통일감.

---

## 4. 컴포넌트 기초

### 4-1. 버튼

**3가지 타입만:**

```jsx
// Primary — 메인 CTA
<button className="btn-primary">길찾기</button>
// 풀-500 배경, 흰 텍스트, 둥근 10px

// Secondary — 보조 행동
<button className="btn-secondary">자세히</button>
// 풀-100 배경, 풀-700 텍스트

// Ghost — 텍스트 버튼
<button className="btn-ghost">취소</button>
// 투명 배경, ink-700 텍스트
```

**크기:**
- `sm` → 32px 높이, 12px 가로 패딩
- `md` → 40px 높이, 16px 가로 패딩 (기본)
- `lg` → 48px 높이, 20px 가로 패딩

**터치 타겟은 최소 40x40px** (접근성)

### 4-2. 풀 카드 (핵심 컴포넌트)

```
┌────────────────────────────────┐
│ 🏊 잠실종합운동장 수영장      ♡│
│ 송파구 · 실내 · 50m           │
│                                │
│ 여기서 1.2km                  │
└────────────────────────────────┘
```

**구조:**
- 배경: `bg-paper`
- 보더: `1px solid line-default` (또는 그림자만)
- 패딩: 20px
- 모서리: `radius-lg` (14px)
- 호버: `shadow-md` 추가 + 살짝 위로 (translateY -2px)
- 찜한 상태: 좌측에 `warm-coral` 색 4px 보더 또는 배지

### 4-3. 입력 필드

- 높이: 44px (터치하기 좋게)
- 보더: `1px solid line-default`
- 포커스: `2px solid pool-500` + `pool-100` 배경 살짝
- placeholder: `ink-400` 컬러

### 4-4. 칩 (필터)

```jsx
<Chip active>실내</Chip>
<Chip>야외</Chip>
```

- 활성: `pool-100` 배경 + `pool-700` 텍스트 + `pool-300` 보더
- 비활성: 흰 배경 + `ink-700` 텍스트 + `line-default` 보더
- 둥글기: `radius-pill`
- 높이: 32px

### 4-5. 마커 (지도)

| 타입 | 컬러 | 모양 |
|---|---|---|
| 실내 풀 | `pool-500` | 동그란 핀 |
| 야외 풀 | `warm-coral` | 동그란 핀 |
| 복합 | `pool-700` | 동그란 핀 |
| 찜한 풀 | + 노란 별 오버레이 | |
| 클러스터 | 풀-100 배경, 숫자 표시 | 둥근 사각 |

---

## 5. 일러스트 & 아이콘

### 5-1. 아이콘 시스템

- **Lucide React** 사용 (https://lucide.dev)
- stroke-width: **1.5px** (기본 2px보다 살짝 얇게 — 부드러움)
- 크기: 16, 20, 24px (시멘틱 사이즈)
- 컬러: 텍스트 컬러 상속 (currentColor)

### 5-2. 일러스트 스타일

- **선 + 단색 면** 조합. 너무 디테일하지 않게
- 컬러는 디자인 토큰 안에서만 (랜덤 컬러 X)
- 외곽선은 `1.5~2px`, `ink-700` 컬러
- 채움색은 *반투명* (예: `pool-200` at 60% opacity)

### 5-3. 빈 상태 일러스트

각 빈 상태마다 일러스트 + 위트 카피:

| 상황 | 일러스트 | 카피 |
|---|---|---|
| 검색 결과 없음 | 빈 풀에 떠있는 튜브 | *이 동네엔 아직 풀이 없네요. 다른 동네는요?* |
| 찜한 풀 없음 | 풀이 (마스코트) 갸우뚱 | *수영 바보 동지님, 아직 찜한 풀이 없네요?* |
| 위치 권한 거부 | 지도 위 물음표 | *위치를 알려주시면 가까운 풀부터 보여드려요.* |
| 로딩 중 | 풀이가 헤엄치는 애니메이션 | *수영장 찾는 중이에요…* |

---

## 6. 마스코트 — *풀이 (Pool-i)*

### 6-1. 캐릭터

- **종류:** 통통한 수달 또는 오리 (둘 중 픽 — 추천: **수달**, 한국 친근감)
- **특징:**
  - 수영모 + 물안경 착용
  - 살짝 졸린 눈, 입꼬리 어색하게 올린 *바보스러운* 표정
  - 둥글둥글 통통한 비율
  - 컬러는 베이지/브라운 + `fool-yellow` 수영모

### 6-2. 사용 규칙

- 빈 상태, 로딩, 온보딩 환영 화면에 등장
- *모든 화면에 있으면 시끄러움.* 핵심 모먼트에만
- 표정 베리에이션:
  - 기본 (졸린 눈)
  - 신난 (눈 반짝)
  - 갸우뚱 (빈 상태)
  - 수영 중 (옆모습)

### 6-3. 절대 하지 말 것

- 마스코트가 사용자에게 *지시하는* 톤 X (*"여기를 누르세요!"* 같은 것)
- 1인칭 대화체 X (*"안녕! 난 풀이야!"* 같은 것)
- 화면 전체를 덮을 만큼 크게 X
- 컬러 다양화 X (베이지 + 옐로 + 풀블루 액센트만)

---

## 7. 모션 & 애니메이션

### 7-1. 타이밍

```css
--ease-snappy:    cubic-bezier(0.4, 0, 0.2, 1);    /* 기본 */
--ease-pool:      cubic-bezier(0.65, 0, 0.35, 1);  /* 물결 느낌 */
--ease-bounce:    cubic-bezier(0.68, -0.4, 0.32, 1.6); /* 찜·확인 */

--duration-fast:   150ms;   /* 호버, 작은 변화 */
--duration-base:   250ms;   /* 표준 */
--duration-slow:   400ms;   /* 모달, 페이지 전환 */
```

### 7-2. 인터랙션 패턴

- **버튼 호버:** scale(1.02) + shadow up. 250ms ease-snappy
- **카드 호버:** translateY(-2px) + shadow-md. 250ms
- **찜 토글:** scale(1) → 1.3 → 1. 400ms ease-bounce
- **페이지 전환:** opacity + 8px slide up. 300ms
- **로딩 (풀이 헤엄):** 좌우 wave 모션, 1.5s loop

### 7-3. 절제 원칙

- 사용자가 *알아채지 못할 정도*가 좋은 모션
- *prefers-reduced-motion* 무조건 존중
- 한 화면에 동시에 움직이는 요소 2개 넘기지 않기

---

## 8. 보이스 & 톤 in UI

### 8-1. 핵심 원칙

| ✅ Do | ❌ Don't |
|---|---|
| *"풀 찾는 중이에요"* | *"검색 중입니다"* |
| *"풀이 없네요"* | *"검색 결과 없음"* |
| *"여기서 1.2km"* | *"거리: 1.2km"* |
| *"또 갈래요?"* | *"다시 시도"* |

### 8-2. 톤 매트릭스

|  | 친근 | 위트 |
|---|---|---|
| **기본 UI** | 80% | 20% |
| **빈 상태** | 70% | 30% |
| **푸시 알림** | 60% | 40% |
| **About 페이지** | 50% | 50% |
| **에러 메시지** | 90% | 10% (에러는 위트 자제) |

### 8-3. 금지

- 이모지 남발 (한 메시지에 1개 이하, 신중히)
- 느낌표 남발 (느낌표 = 신난 분위기, 적재적소만)
- 자기 어필 (*"우리는 …"*, *"저희가 …"*) 거의 안 씀
- 영어·한국어 무분별 섞기 — *Pool's Day*는 OK, *user는 swim 좋아함* 같은 건 X

---

## 9. 접근성

- **명도 대비:** 본문 텍스트는 배경과 4.5:1 이상 (WCAG AA)
- **터치 타겟:** 최소 40x40px (Apple HIG는 44px 권장)
- **포커스 인디케이터:** `2px solid pool-500` outline + 2px offset
- **alt 텍스트:** 모든 일러스트·마스코트 이미지에 의미있는 설명
- **키보드 네비:** Tab 순서 자연스럽게, ESC로 모달 닫기
- **색상에만 의존 X:** 실내·야외 구분도 아이콘/텍스트로 보강

---

## 10. 응용 예시

### 10-1. 헤더

```
배경: bg-cream
높이: 64px (모바일) / 72px (데스크톱)
좌측: 로고 "Pool's Day" (Fraunces italic 700, pool-700) + 풀이 아이콘
우측: 검색 아이콘 + 찜 아이콘 (line, 1.5 stroke)
보더: 하단 1px line-subtle
```

### 10-2. 풀 카드 적용

```
┌──────────────────────────────────────┐
│                                      │ ← bg-paper, padding 20px
│  잠실종합운동장 수영장        ♡     │ ← h4 (Pretendard 600 17px)
│                                      │
│  [실내]  송파구 · 50m              │ ← 칩 + caption
│                                      │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │ ← 1px line-subtle
│                                      │
│  여기서 1.2km     >                 │ ← caption (ink-500)
│                                      │
└──────────────────────────────────────┘
   radius-lg (14px), shadow-sm
   호버: shadow-md + translateY(-2px)
```

### 10-3. 빈 상태

```
        🦦 (풀이, 갸우뚱)
        
   수영 바보 동지님,
   아직 찜한 풀이 없네요?
   
   [   풀 둘러보기   ]    ← btn-primary
```

---

## 11. 체크리스트 (PR 머지 전)

- [ ] 컬러는 모두 CSS 변수로 (하드코딩 hex X)
- [ ] 폰트 사이즈는 정의된 스케일에서만
- [ ] 패딩·마진은 8의 배수
- [ ] 모서리는 정의된 `--radius-*` 사용
- [ ] 그림자는 풀 블루 톤 그림자 사용
- [ ] UI 카피는 *친근·위트* 톤 체크
- [ ] 명도 대비 4.5:1 이상
- [ ] `prefers-reduced-motion` 대응
- [ ] 터치 타겟 40px 이상

---

## 12. 영감 레퍼런스

이 결을 잡을 때 참고하면 좋은 서비스들:

- **Headspace** — 따뜻한 색감 + 캐릭터 친화 + 매일 쓰는 앱 톤
- **Linear** — 미니멀하지만 차갑지 않은 균형
- **Notion** — 한·영 혼용, 이모지·일러스트 절제된 사용
- **Duolingo** — 마스코트 활용 (단, Duolingo는 더 시끄러움 — 우리는 더 차분)
- **Olipop (음료)** — 레트로 위트 + 정돈된 럭셔리

---

*이 문서를 따라 만들면, Pool's Day는 어디에 놓여도 *Pool's Day*처럼 보일 거예요.*
